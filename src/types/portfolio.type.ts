export interface Portfolio {
  _id: string;
  title: string;
  description: string;
  category: string;
  image?: string;      // Placeholder for now
  projectUrl?: string;
  createdAt?: string;
  updatedAt?: string;
}
